/** Base URL for all API requests. Must include the version prefix. */
export const API_BASE_URL = 'http://localhost:3000/api/v1';

/** Abort timeout for each fetch request (milliseconds). */
export const REQUEST_TIMEOUT_MS = 10_000;
